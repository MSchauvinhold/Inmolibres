export const metadata = { title: "Índices ICL/IPC — Admin" };

import { AdminIndicesClient } from "@/components/admin/AdminIndicesClient";

export default function AdminIndicesPage() {
  return <AdminIndicesClient />;
}
